"use strict";
class Item
{
    constructor(properties)
    {
        this.name = properties["name"] || "Bad Item".
        this.description = properties["description"] || "Where did you even find this?";
    }
}
